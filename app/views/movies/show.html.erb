<h1><%= @movie.title %></h1>

<%= button_to 'Discover Page', user_discover_path(@user), method: :get %>

<%= button_to "Create Viewing Party for #{@movie.title}", new_user_movie_party_path(@user, @movie.id), method: :get %>

Vote Average: <%= @movie.vote_average %> - Runtime: <%= @movie.runtime %> - Genre(s): <%= @movie.genres %>